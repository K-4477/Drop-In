<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.LocationApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>
    <style name="ThreeDotsSpinnerStyle" parent="Widget.AppCompat.Spinner">
        <item name="android:dropDownWidth">wrap_content</item>
        <item name="android:dropDownVerticalOffset">10dp</item>
        <item name="android:dropDownHorizontalOffset">0dp</item>
        <item name="android:drawable">@drawable/ic_accessible</item>
        <item name="android:popupElevation">4dp</item>
    </style>
    <style name="Theme.LocationApp" parent="Base.Theme.LocationApp" />
</resources>